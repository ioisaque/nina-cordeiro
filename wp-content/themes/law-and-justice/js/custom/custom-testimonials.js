//Use Strict Mode
(function ($) {
    "use strict";

    //Begin - Window Load
    $(window).load(function () {

        $(".testimonial-carousel").owlCarousel({
            items: 1,
            nav: true,        	
        });

    });

    //End - Use Strict mode
})(jQuery);